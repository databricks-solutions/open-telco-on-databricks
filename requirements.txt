# Dependencies for the OTel notebooks, which run on Databricks.
# On the Databricks ML runtime torch + transformers are already installed; on serverless,
# run the first `%pip install` cell in each notebook (it installs these and restarts Python).

sentence-transformers>=3.0.0
transformers>=4.40.0        # Chapter 1 reranker (OTel-Reranker-0.6B, trust_remote_code=True)
# torch is pulled in by sentence-transformers.

# numpy 2 can break some DBR/GPU repls; pin low if you hit issues on a cluster.
# numpy<2

# Optional — only if you'd rather open the notebooks in a local Jupyter than in the workspace:
# jupyterlab>=4.0.0
